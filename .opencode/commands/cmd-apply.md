---
description: Apply the suggestions from the previous command
---

Re-read the previous response in full to identify every concrete suggestion. Implement each one as a file edit, working sequentially from most impact to least.

After all edits are applied, run `ruby scripts/validate_content.rb` and fix any new failures. If the validator introduces failures unrelated to these changes, leave them alone and note them.
